# Package Annotations 

This package contains annotations that can be used to automatically send notifications to Telegram.

The main annotations are:
- `@NotifyOnFailure` - sends a message on failure
- `@NotifyOnSuccess` - sends on success (if implemented)